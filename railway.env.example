# Railway Environment Variables Template
# Copy this file and set the actual values in Railway dashboard

# Application Configuration
NODE_ENV=production
PORT=3000
API_PREFIX=api/v1
CORS_ORIGIN=*

# Database Configuration (Railway PostgreSQL)
# Railway automatically provides these variables when you add a PostgreSQL service
# You can either use the Railway template variables or the direct environment variables
DB_HOST=${{Postgres.PGHOST}}
DB_PORT=${{Postgres.PGPORT}}
DB_USERNAME=${{Postgres.PGUSER}}
DB_PASSWORD=${{Postgres.PGPASSWORD}}
DB_DATABASE=${{Postgres.PGDATABASE}}

# Alternative: Railway also provides these direct environment variables
# PGHOST=${{Postgres.PGHOST}}
# PGPORT=${{Postgres.PGPORT}}
# PGUSER=${{Postgres.PGUSER}}
# PGPASSWORD=${{Postgres.PGPASSWORD}}
# PGDATABASE=${{Postgres.PGDATABASE}}

# Redis Configuration (Railway Redis)
REDIS_HOST=${{Redis.REDIS_HOST}}
REDIS_PORT=${{Redis.REDIS_PORT}}
REDIS_PASSWORD=${{Redis.REDIS_PASSWORD}}

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRES_IN=24h

# Email Configuration (SMTP)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=your-email@gmail.com
MAIL_PASS=your-app-password
MAIL_FROM=noreply@4ami.com

# Application URLs
FRONTEND_URL=https://your-frontend-domain.com
BACKEND_URL=https://your-railway-app.railway.app

# AI Agent Configuration
AI_AGENT_URL=https://your-ai-agent-url.com
AI_AGENT_API_KEY=your-ai-agent-api-key

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# pgAdmin Configuration (if using)
PGADMIN_EMAIL=admin@4ami.com
PGADMIN_PASSWORD=your-pgadmin-password
